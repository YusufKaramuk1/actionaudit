name: Dispatch Safe
on:
  workflow_dispatch:
    inputs:
      target:
        description: deployment target
        required: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - env:
          TARGET: ${{ inputs.target }}
        run: echo "Deploying to $TARGET"
