name: Safe Taint
on: pull_request
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      BASE: ${{ github.event.pull_request.base.sha }}
    steps:
      - run: echo "BASE=$BASE"
