name: Safe Taint
on: pull_request
permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PR_TITLE: ${{ github.event.pull_request.title }}
    steps:
      - run: echo "PR title is $PR_TITLE"
