name: Safe PR Target
on:
  pull_request_target:
    branches: [main]

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.base.sha }}
      - run: echo "labeling"
